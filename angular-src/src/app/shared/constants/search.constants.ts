/**
 * Search-related constants used across the application.
 */

/**
 * Standard debounce time for search inputs in milliseconds.
 * Applied to all search bars to ensure consistent user experience.
 */
export const SEARCH_DEBOUNCE_TIME = 400;

/**
 * Minimum search term length before triggering a search.
 * Can be used to prevent searching with very short terms.
 */
export const MIN_SEARCH_LENGTH = 0;
